# GCFIS — changelog (version-only)
s45  baseline v40 audit + GCFIS 13-layer spec
s46  change-core + first engines (fragility, forward-macro, accumulation, lead-lag, meta) + real-data validation
s47  full 13-layer GCFIS + Entry engine (gamma-aware, risk-range) + dead-code prune 122→103
s48  cross-asset coherence + per-ticker reason narrative + entry defer-gate + GCFIS dashboard renderer
s49  GCFIS wired into app — new 🧭 GCFIS tab (router + snap-adapter, defensive), sidebar caption trimmed to version-only
s50  product-confluence ranking (geomean theme×bottleneck×accumulation×adoption×reflexivity) + bottleneck node→ticker mapping + Reflexivity engine (B5) + full per-ticker output contract (Options/Macro/Opportunity scenarios/Institutional) + multi-panel card
s51  complete output contract: Options full greek set (gamma + charm + gamma_flip + vanna), Scores panel adds liquidity/dealer/positioning, Institutional threads revision/ownership_Δ/etf_flow + fundamentals into accumulation crowding
s52  lead-lag graph wired INTO selection (rotation engine: fired-leader → primed-follower conviction boost + timing window) — the GCFIS 'moat', previously decorative; portfolio concentration guard (correlation clustering → effective bets + alloc multiplier so a correlated cluster isn't oversized); leadlag_cfg passthrough
s53  ALL 4 structural gaps closed + GCFIS now VISIBLE/POPULATED: (1) real Gaussian HMM regime engine (regime_hmm.py, hmmlearn, runtime-fit, graceful fallback) replaces quad-lookup posterior; (2) bottleneck migration tracker (emerging/fading node by Δtightness) wired into orchestrator output; (3) adapter ENRICHED — harvests v40's real data (volumes, breadth, VIX, GEX/walls→dealer_by_ticker override, quads) so panels populate instead of n/a (v40 GEX labelled proxy, not faked); (4) GCFIS confluence section folded into Alpha Center (_gcfis_inline, guarded). requirements: +hmmlearn
s54  FIX long-only directional bug (the BREN.JK screenshot): rich_ticker_card _gex_levels_chart drew a SHORT layout (Entry at resistance ABOVE px, target BELOW entry) on buy-only IDX names, ignoring the already-correct WAIT recommendation → now long-only aware (bearish IDX → range/flat, Entry below px, target ABOVE entry, 'buy-only' title) + stale-entry guard ('harga sudah lewat entry'); _directional_levels + compute_optimal_entry + build_options_recommendation all long-only guarded. GCFIS: market registry (markets.py — doc 5: per-market long_only/drivers/bottlenecks) + entry long_only short→AVOID + orchestrator avoided_long_only bucket + market stamped per signal + dashboard AVOID section.
s55  docs 1-9 COMPLETE: flow_type engine (4-way ACCUMULATION/DISTRIBUTION/SHORT_COVERING/PANIC + absorption/efficiency/persistence/resilience, OHLCV-proxy labelled) + market_mode engine (PINNING/EXPANSION/SQUEEZE/DISTRIBUTION → execution style) + Stage-1 elimination (MAD-robust gaps/vol-of-vol/false-breakout) + response_zone (TRR/LRR as response zones) + decision_stack per ticker (Category/Why-Now/Who's-Trapped/Invalidation/Execution+size+holding+target-map) + market-weighted confluence (per-market driver weights, doc 5) + doc-4 sections (Early Monsters/Squeeze/Tactical/MeanRev/Distribution Warning) + researched MARKET DRIVER MAP (market_drivers.py + MARKET_DRIVERS.md: surge-up/down per market, data series, horizon ST/MT/LT, strength, Jun-2026 evidence) wired into tab with live z-readings from available feeds
